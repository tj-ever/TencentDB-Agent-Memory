# 飞书机器人开通清单（二开标准）

新建项目机器人按本清单执行。机器人定位：产品经理 agent，问答 + 文档原型交付。权限基线 = **47 个 scope**（卓驭/阳光/海大三应用已统一）。

## 一、权限基线（47 个 scope）

### bridge 核心链路

| scope | 用途 | 代码依据 |
| --- | --- | --- |
| `im:message` + `im:message:send_as_bot` | 发送回复消息（含卡片消息体） | `channel.send()`，每条回复 |
| `im:message.p2p_msg:readonly` | 长连接接收用户单聊消息 | 事件 `im.message.receive_v1` |
| `im:message.group_at_msg:readonly` / `im:message.group_at_msg.include_bot:readonly` / `im:message.group_msg.include_bot:read` | 群聊 @ 消息接收 | 同上，群聊场景 |
| `cardkit:card:write` | 创建/增量更新打字机流式卡片 | 缺失时机器人无 ack |
| `im:message:readonly` | reject 时反查消息 mentions 做诊断 | `runtime.ts:298` |
| `application:application:self_manage` | 权限自审计（见第三节） | `applications/me` |
| `im:chat:read` | 群信息（SDK 群聊能力基础） | — |
| `im:chat:create` / `im:chat:update` / `im:chat.members:bot_access` | 建群/改群/获取群成员 | — |
| `im:message:update` / `im:message:send_multi_users` / `im:message:send_sys_msg` | 撤回/批量发/系统消息 | — |
| `im:message.pins:*` / `im:message.reactions:*` | Pin / 表情回应 | — |
| `application:app_slash_command:read/write` / `application:bot.basic_info:read` / `application:bot.menu:write` | 机器人基础信息与菜单 | — |
| `contact:contact.base:readonly` | 用户基础信息 | — |

### 文档链接自动提权（bridge 对所有 bot 生效）

回复中出现文档链接时自动调协作者/公开设置接口（`docShare.ts`），缺失则外链用户打不开文档。官方接受 `drive:drive`（推荐）或 `docs:permission.member:create` 等任一：

| scope | 用途 |
| --- | --- |
| `drive:drive` | 查看、评论、编辑和管理云空间所有文件 |
| `docs:permission.member:create` | 添加协作者 |

### PM 文档交付链（claude 子进程直连 OpenAPI）

| scope | 用途 |
| --- | --- |
| `docx:document:create` / `docx:document:readonly` | 建文档/查结构 |
| `docx:document` / `docx:document:write_only` / `docx:document.block:convert` | 写文档块（正文填充） |
| `docs:document.comment:*`（create/delete/read/update/write_only） | 文档评论 |
| `docs:document.media:upload` | 文档内图片/媒体上传 |
| `sheets:spreadsheet` / `sheets:spreadsheet:create` | 原型附表 |
| `drive:file` / `drive:file:upload` / `drive:drive.metadata:readonly` | 附件块/文件交付 |
| `wiki:node:read` | 知识库节点读取 |

### 预留（当前未调用，随基线保留）

`cardkit:card:read`（卡片读取）、`im:resource`（识图前置）、`offline_access`（用户 OAuth 刷新令牌前置）。

## 二、开通步骤（飞书开放平台控制台，手工）

CLI/MCP 均无法修改应用权限（飞书未开放该 API，OAuth 授权也只作用于已勾权限之内），以下必须控制台操作：

1. [开放平台](https://open.feishu.cn/app) → 创建企业自建应用，记下 `app_id` / `app_secret`
2. 添加应用能力 → **机器人**
3. 权限管理 → 按第一节清单勾选（免审，即开即生效）
   - 批量开通链接（逗号分隔，单次 ≤10 个，过多报参数不合法）：
     `https://open.feishu.cn/app/<app_id>/auth?q=<scope1>,<scope2>&op_from=openapi&token_type=tenant`
4. 事件与回调 → 订阅方式选**长连接**，订阅事件：
   - `im.message.receive_v1`（接收消息）
   - `im.message.recalled_v1`（消息撤回——bridge 主动中止生成依赖它）
5. 版本管理与发布 → 创建版本并发布（个人版免审自动通过）

## 三、权限审计（API 精确查询）

```bash
# 凭据来自 bridge 的 /app/data/bots.json
token=$(curl -s -X POST https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal \
  -H 'Content-Type: application/json' -d '{"app_id":"<appid>","app_secret":"<secret>"}' \
  | python3 -c 'import json,sys;print(json.load(sys.stdin)["tenant_access_token"])')
curl -s "https://open.feishu.cn/open-apis/application/v6/applications/me?lang=zh_cn" \
  -H "Authorization: Bearer $token"   # data.app.scopes = 全量已开通 scope
```

- 缺 scope 调 API 会报 99991672，报错自带该权限的开通链接
- 行为探测辅助：代表 API 返回 99991672 = 缺权限，参数校验类错误（1063001/99992402）= 已开通

## 四、注册步骤（bridge 侧，可脚本化）

凭据、端口等运维值见服务器 `bots.json` 与面板，**不要写入本仓库**：

1. 面板建 Team/Agent（已有则复用），记 `team_id` / `agent_id`
2. 创建 Task 绑定：`POST http://localhost:8125/api/v1/meta/task/create`，头 `x-tdai-service-id: default` + `x-tdai-user-key: <admin key>`，体 `{team_id, creator_user_id, title, description, source_type:"manual", linked_agents:[{agent_id}]}` → 记 `task_id`
3. 注册机器人：`ssh ubuntu` → `POST http://localhost:8130/api/bots`，头 `x-bridge-token`，体含 `name` / `work_dir:/app/workspaces/<项目>` / `memory{proxy_base_url,space_id,user_key}` / `binding{team_id,agent_id,task_id}` / `feishu{app_id,app_secret,stream_initial_text,policy}` / `session_mode:"user"` / `system_prompt`（只写项目特有内容——人设、专属技能引用；交付、原型生成与质量规则由 bridge 统一注入基线）
4. 启动：`POST /api/bots/<bot_id>/start`（创建后不会自动 running）
5. 装通用技能包：`ssh ubuntu` → `~/.nvm/versions/node/v24.19.0/bin/node ~/skill-packages/deploy-skills.mjs <team_id> <agent_id>`（25 个 PM 交付/研究技能从卓驭沉淀的通用包分发到新 team；管理规范见服务器 `~/skill-packages/README.md`）
6. 验证：`docker logs tdai-memory-bridge` 应看到 `resolveUpstream` 模型解析成功、`ws client` ready、`botIdentity` 正常；飞书私聊发一句话确认有 ack 卡片；再发一条让它建文档，确认返回可打开的链接；最后用第三节脚本核验 47 scope 齐全

## 五、存量应用（2026-09-01 审计）

| 应用 | app_id | scope 数 |
| --- | --- | --- |
| 卓驭 | cli_aaf474ef88785d21 | 47 |
| 阳光 | cli_aae6a4fd58b85d2e | 47 |
| 海大 | cli_aa1c8b0d910bdbcf | 47 |

三应用权限完全一致。
