# 二开说明：飞书机器人渠道（MemoryBridge）

> 本仓库是在 [TencentDB-Agent-Memory](https://github.com/TencentCloud/TencentDB-Agent-Memory) v2.0.1-beta.2 基础上的二次开发。
> 二开内容：新增 **MemoryBridge** 飞书渠道服务 + **MemoryPanel 飞书机器人管理页**，把腾讯 Mem 记忆系统接入飞书。
> 原型来源：`~/Desktop/project/ygxny/pc/fs`（单机器人 YAML 版），本仓库已升级为多机器人面板托管版。

---

## 1. 架构与数据流

```
飞书用户 ⇄ MemoryBridge:8130（飞书长连接，每机器人一个实例）
                └-> 本机 claude -p（headless，注入记忆绑定 header）
                      └-> MemoryProxy:8096/claude-code/<space_id>
                            └-> MemoryCore（记忆 + 知识库工具注入）
面板 8125 ──/api/v1/channels 反代──> MemoryBridge HTTP API
```

每个机器人绑定一个 **Team / Agent / Task**（记忆隔离维度）+ 一个 `work_dir`（claude 的 cwd，文件产出隔离）。`x-conversation-id` 使用飞书发送者的 open_id，记忆按用户隔离。

## 2. 二开改动的文件清单

### MemoryBridge（新增子项目，TypeScript，与 MemoryPanel 同栈：ESM + strict + tsx + vitest）

| 文件 | 职责 |
|---|---|
| `src/index.ts` | 入口：起 HTTP API，自动拉起 `enabled=true` 的机器人 |
| `src/http.ts` | HTTP API（原生 http，含 CORS） |
| `src/store.ts` | 配置持久化 `data/bots.json`；必填校验、密钥脱敏（`publicBot`/`maskSecret`） |
| `src/runtime.ts` | 机器人生命周期（startBot/stopBot/statusOf）、消息处理主循环、文档自动提权 |
| `src/claudeRunner.ts` | spawn `claude -p`（stream-json），注入 `x-*` header + 飞书 env；上游断连重试 2 次 |
| `src/sessionMode.ts` | 会话模式：`none` 逐条新开 / `user` 按人续聊 / `chat` 按群续聊 |
| `src/streamRotate.ts` | 打字机卡片轮换（飞书单卡约 10 分钟关流，到点收卡续卡） |
| `src/docShare.ts` | 识别回复中的飞书 docx/sheet 链接，自动提权为「链接可读可编」+ 域名改写 |
| `scripts/embed-prototype.mjs` | 给飞书文档嵌入 HTML 原型截图（puppeteer headless-shell 截图，npm 自带内核；供 claude 直接 node 调用，保持 .mjs） |

### MemoryPanel（修改）

| 文件 | 改动 |
|---|---|
| `src/panel/config/panel-config.ts` | 新增 `bridge.baseUrl`（env `MEMORY_BRIDGE_URL`，默认 `http://127.0.0.1:8130`） |
| `src/panel/http/app.ts` | 注册 `registerChannelRoutes` |
| `src/panel/http/routes/channels.ts` | **新增**：`/api/v1/channels/*` 鉴权 + 反代 Bridge |
| `web/src/pages/team/ChannelsPage/index.tsx` | **新增**：机器人管理页（列表/新建/编辑/启停/删除） |
| `web/src/lib/api/channels.ts` | **新增**：前端 API 封装 |
| `web/src/constants/menu.tsx`、`layouts/ConsoleLayout.tsx`、`routes/index.tsx` | 菜单项 `feishu_bots` + 路由 `/team/feishu-bots` |
| `web/src/i18n/{zh-CN,en-US}.ts` | `channels.*` / `menu.feishu_bots` 文案约 90 条 |

## 3. 启动与配置

```bash
# 一条命令全栈（推荐，零宿主机依赖）
cd deploy/global-images && cp .env.example .env
# .env 里把 BRIDGE_ENABLED=1，然后：
./start-all.sh        # 三件套 + 第 4 个容器 tdai-memory-bridge（本地构建镜像）

# 或只补起 bridge：
./start-memory-bridge.sh
```

容器部署细节（`MemoryBridge/Dockerfile` + `deploy/global-images/start-memory-bridge.sh`）：

- 镜像自包含：claude CLI（`@anthropic-ai/claude-code`）+ chrome-headless-shell 截图内核 + `fonts-wqy-zenhei` 最小中文字体（~9MB，原型文字可渲染）
- 加入 `tdai-memory-stack` 网络：bridge -> `http://tdai-proxy:8096`；面板容器注入 `MEMORY_BRIDGE_URL=http://tdai-memory-bridge:8130`
- 三个 volume：`tdai-memory-bridge-data`（bots.json）、`-workspaces`（机器人工作目录）、`-sessions`（/root/.claude 会话，重启不丢）
- `store.ts` 的 proxy 兜底地址由 `BRIDGE_PROXY_DEFAULT` 注入（容器内为 `http://tdai-proxy:8096`），机器人表单留空即用兜底
- `stop-all.sh` 已同步管理 bridge 容器与三个 volume（含 --purge）

本机开发直跑（需要宿主机有 claude CLI）：

```bash
cd MemoryBridge && npm install && npm start   # :8130
```

Bridge 自身环境变量：`BRIDGE_PORT`(8130)、`BRIDGE_DATA_DIR`(./data)、`CLAUDE_BIN`(claude)。原型截图走 puppeteer 自带 Chromium（推荐 `PUPPETEER_SKIP_CHROME_DOWNLOAD=1 npm install` 只装 headless-shell 内核，约 100MB），不依赖宿主机浏览器。开发命令：`npm run dev`（tsx watch）、`npm run typecheck`、`npm test`（vitest）、`npm run build`（tsc -> dist/）。

机器人配置全部在**面板 → 组织与权限 → 飞书机器人**页面维护，存于 `MemoryBridge/data/bots.json`（data 目录已 gitignore，含密钥勿提交）。字段：

| 字段 | 说明 |
|---|---|
| name / work_dir | 机器人名（进提示词）、claude 的 cwd |
| memory.* | proxy 地址 / space_id / user_key（面板建用户时返回） |
| binding.* | team_id / agent_id / task_id（记忆绑定，面板详情页抄） |
| feishu.* | app_id / app_secret、`policy.requireMention`（群里需 @）、`policy.dmMode`、`stream_initial_text` |
| llm.model | proxy 上游模型名（默认 grok-4.6） |
| session_mode | none / user / chat |
| system_prompt | 自定义系统提示词，留空用内置通用规则 |

## 4. Bridge HTTP API（面板反代的就是这些）

- `GET /health`
- `GET /api/bots?team_id=`
- `POST /api/bots` / `PUT /api/bots/:id` / `DELETE /api/bots/:id`
- `POST /api/bots/:id/start` / `POST /api/bots/:id/stop`

密钥写入仅在 create/update，列表/详情接口脱敏（`app_secret` 恒 `********`，`user_key` 前 8 位）。更新时密钥传掩码即「保持原值」。

## 5. 与原版（fs 项目）的差异及恢复方法

1. **内置提示词泛化**：原版 `claudeRunner.js`（源项目）内置「PRD 生成 → 自包含 HTML 原型 → 飞书建文档 → 嵌截图」完整流水线规则；本版换成通用规则。`embed-prototype.mjs` 已迁移但无提示词驱动。恢复方式：把原版规则贴到机器人的 system_prompt 字段（或加回 `defaultRules`），claude 会通过 `FEISHU_EMBED_PROTOTYPE` 环境变量找到脚本。
2. **文档自动提权行为变化**：原版对配置了自定义 system_prompt 的机器人跳过自动提权；本版 `runtime.ts#shareDocs` 无条件执行。需还原则在该函数开头加 `if (bot.system_prompt) return;`。
3. **未迁移项**：`streamRotate.test.js`、`docShare.test.js`（测试）、`scripts/register-scopes.js`（权限注册工具）、原 `docs/` 6 篇设计文档、业务提示词 `yangguang.system.md` 及 `workspaces/` 业务目录（属源项目业务，不迁移）。
4. **改进项**：YAML 单机器人 → 多机器人 HTTP 托管；`CLAUDE_BIN` 从硬编码 nvm 路径改为环境变量；Chrome 内核改为 puppeteer 自带（npm install 下载），不再依赖宿主机浏览器。

## 6. 二开扩展指引（KISS / YAGNI 原则）

- **加配置字段**：`store.ts#normalize`（写入+默认值）→ `publicBot`（对外输出）→ `runtime.ts#startBot`（消费）→ `ChannelsPage` 表单 + `i18n` 两份文案。四步即闭环，勿在 Bridge 里做面板已能做的事。
- **加消息通道**（企微/钉钉）：仿 `runtime.ts` 的 channel 抽象，`claudeRunner`/`streamRotate`/`sessionMode` 与飞书 SDK 解耦，可直接复用。
- **改内置提示词**：`claudeRunner.ts#defaultRules`。业务专属规则优先走面板 system_prompt 字段，不要堆进默认规则。
- **面板对接其他自建服务**：仿 `channels.ts` 的「鉴权 + fetch 反代 + 503 降级」模式，配置统一进 `panel-config.ts`。
- **跑测试**：`cd MemoryBridge && npm test`（vitest，与 MemoryPanel 一致）。

## 7. 升级上游版本时的合并要点

上游发新版后合并本仓库时，重点检查三处：

1. `MemoryPanel/src/panel/http/app.ts` 的 `registerChannelRoutes` 注册行是否保留；
2. `MemoryPanel/src/panel/config/panel-config.ts` 的 `bridge` 段是否保留；
3. `web/` 下菜单/路由/i18n 是否冲突（`PageId` 联合类型新增了 `feishu_bots`，上游若改动 menu.tsx 需手工合）。

MemoryBridge 为独立目录，上游不存在，无合并冲突风险。

---
*文档生成：2026-08-20，基于 commit `3660617` + 工作区未提交的面板集成改动。*
